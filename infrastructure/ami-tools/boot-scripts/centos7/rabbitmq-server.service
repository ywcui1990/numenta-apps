[Unit]
Description=RabbitMQ
After=network.target syslog.target

[Install]
Alias=rabbitmq-server.service
WantedBy=multi-user.target

[Service]
Type=notify
ExecStart=/usr/sbin/rabbitmq-server
ExecStop=/usr/sbin/rabbitmqctl stop
Restart=always
