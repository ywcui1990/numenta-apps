# Numenta Unicorn: HTM Engine

This `backend/` directory is for ModelRunner and Support (Python / C++).
This functionality is exposed to the user via code in the sibling `../gui/`
directory.

See: `requirements.txt`
