# Numenta Unicorn: Frontend + GUI

This `frontend/` directory is for the GUI (Graphical User Interface) and
Model Management code. This exposes the functionality of the
sibling `../backend/` directory.

See: `../package.json`
