{
  "description": "--stats arg passed to the engine; used by getScalarMetricWithTimeOfDayParams in htmengine/algorithms/...",
  "type": "object",
  "additionalProperties": false,

  "properties": {
    "min": {
      "description": "Min metric data value",
      "required": true,
      "type": "number"
    },
    "max": {
      "description": "Max metric data value",
      "required": true,
      "type": "number"
    },
    "minResolution": {
      "description": "Minimum resolution of metric",
      "required": false,
      "type": "number"
    }
  }
}
